version: 1
backend:
  phases:
    build:
      commands:
        - nvm install 20.12.0
        - nvm use 20.12.0
        - echo "AMPLIFY_DATA_DEFAULT_NAME=$AMPLIFY_DATA_DEFAULT_NAME" >> .env
        - echo "API_ENDPOINT=$API_ENDPOINT" >> .env
        - echo "AUTHORIZATION_TYPE=$AUTHORIZATION_TYPE" >> .env
        - echo "AMPLIFY_DATA_GRAPHQL_ENDPOINT=$AMPLIFY_DATA_GRAPHQL_ENDPOINT" >> .env
        - echo "AMPLIFY_DATA_MODEL_INTROSPECTION_SCHEMA_BUCKET_NAME=$AMPLIFY_DATA_MODEL_INTROSPECTION_SCHEMA_BUCKET_NAME" >> .env
        - echo "AMPLIFY_DATA_MODEL_INTROSPECTION_SCHEMA_KEY=$AMPLIFY_DATA_MODEL_INTROSPECTION_SCHEMA_KEY" >> .env
        - cat .env
        - npm config set registry https://registry.npmmirror.com
        - npm install -g pnpm
        - pnpm install --no-frozen-lockfile --ignore-scripts
        - pwd && ls
        - cd amplify/functions/autoSendServer/src &&  pnpm install --no-frozen-lockfile --ignore-scripts  && cd -
        - cd amplify/functions/saveKakaoLoginInfo/src && pnpm install --no-frozen-lockfile --ignore-scripts && cd -
        - cd amplify/functions/startTalkSender/src && pnpm install --no-frozen-lockfile  --ignore-scripts  && cd -
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID

frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.12.0
        - nvm use 20.12.0
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .npm/**/*
      - dist/**/*
